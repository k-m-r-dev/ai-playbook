# Universal AI Development Template (React Native Monorepo)

Stack profile for React Native monorepos (apps + shared packages).

## Commands (customize per project)

```bash
cd apps/<mobile-app>
yarn test
yarn lint
```

## Core principles

- Keep app and shared package changes explicit.
- Verify critical flows on device/emulator before merge.
