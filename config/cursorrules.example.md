# Cursor system constraint directives (legacy .cursorrules)

Optional: copy to repository root as `.cursorrules` if you do not use `.cursor/rules/` only.

Prefer the installable overlay: `.cursor/rules/05-unified-ai-framework.mdc` and siblings from `--platform universal`.

- **Context awareness:** Before complex work, read `CLAUDE.md` (ledger) and `ARCHITECTURE.md`.
- **Token budget:** Do not run wide repository text searches. Use graphify MCP or `graphify-out/GRAPH_REPORT.md` to walk node relationships.
- **Cross-session continuation:** At the end of major architectural work, output a 3-bullet summary for the user to paste into `CLAUDE.md` Cross-Session Learnings.
