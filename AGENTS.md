## Learned User Preferences

- Prefer grill-style clarification (one question at a time, plain-English explanation per option, `[recommended]` default) before ambiguous skill, template, or client-configuration work; for W2C/work-to-chores grilling, gather repo evidence first and present facts, edge cases, and current patterns before asking; target about four options with concise pros/cons, examples, outcomes, and mermaid when helpful (fewer or more—including yes/no—when the decision warrants it; never pad to hit four); write why the chosen approach won plus pros/cons/outcome/diagrams into the milestone CONTEXT; after configure/bootstrap, flag remaining gaps and ask whether to apply each rather than leaving leftover merge-yourself notes; client configure chooses exactly one planning engine — GSD, W2C, or none (overlay only) — never both (W2C means PATH `w2c` from OpenW2C/w2c plus `w2c init`, not wrappers-only); when the user requests GSD-only configure, skip AI overlay by default and only walk remaining GSD/workflow gaps; when switching a client to W2C, grill leftover GSD uninstall (`.gsd`, GSD MCP, `.workflow`) as in-scope, prefer archive plus port of incomplete GSD milestones into `.w2c/plans/` so `do-chores` can continue (archive-only / `imported-from-gsd` is not continuity), then strip do-next/`gsd-*` skills and wiring while keeping project-specific knowledge; prefer unlinking the client `.gsd` symlink and leaving `~/.gsd/projects/<id>` as backup unless the user asks to delete it; W2C planning grills worktree vs branch (no in-place) and a ticket-id or slug remote branch; W2C `init`/migrate must grill a required non-skippable git publish cadence with no template default — prefer slice-complete local commit and milestone-complete push/PR, and require explicit user approval for both commit and push; never create empty `CONTEXTvN.md` — skip if nothing to record, fill the latest stub, and bump only when the previous CONTEXT already has real content.
- Do not edit symlinked overlay files from ai-playbook in client repos; overlay `_AGENTS.md` / `_CLAUDE.md` stay architecture-only (no GSD, W2C, graphify, ruflo, or other-client learned facts); committed wrappers include those and mention only the selected planning engine — GSD/openGSD/graphify/ruflo text only when engine is GSD; W2C or none must not ship those signatures; continual-learning stays in project-local wrapper sections.
- Continual-learning should maintain only project-local `AGENTS.md` learned sections — never overwrite playbook-owned `_AGENTS.md` policy content.
- Prefer a short plan and explicit approval before permanent cross-client fixes.
- Client installs must preserve IP isolation: never ship ai-playbook source content into client repositories.
- For GitHub multi-account on client configure, prefer per-repo direnv `.envrc` (not `.env`) via `configure-client-git-account.sh`; `configure-client-project` grills gh account on init and checks existing repos; prefer `.envrc` that `source_env_if_exists .envrc.local` with `GH_TOKEN="$(gh auth token -u USER)"` in gitignored `.envrc.local` over a pasted static token (stale `GH_TOKEN` overrides keyring and breaks `gh`); configure/check scripts must treat that sourced `.envrc.local` pattern as satisfied — do not require a playbook `GH_TOKEN` block inside `.envrc` itself; alternatives (global gh wrapper, manual auth switch, SSH/`includeIf`) live in `docs/github-multi-account.md`; optional machine registry at `~/.config/ai-playbook/git-accounts.toml` (from `config/git-accounts.example.toml`).
- Prefer agent-driven do-next / do-next-runner across Cursor, Claude CLI, and oh-my-pi (gsd-pi as ledger/MCP) via gsd-workflow plus the playbook claim bridge; avoid unsupervised full `gsd_execute` / Copilot billing unless the user opts in.
- Prefer playbook-owned GSD claim/publish bridges over waiting on upstream gsd-pi PRs when using gsd outside their app; comfortable with GSD's projected plan files if ticket-to-plan → do-next stays reviewable (not locked to phases `PLAN.md` checkboxes).
- When configuring client repos, ensure `.gitignore` covers overlay-generated or local-engine artifacts that should not be committed.
- Before editing Claude or Copilot skill surfaces on platform templates, check whether they are symlinked to Cursor; update only real files and leave symlinked surfaces alone.
- For multi-stack monorepos, prefer `--platform universal` and the planning engine at the repo root; nested Flutter (or other) packages get their platform overlay only — no engine — unless the user asks to install an engine there.
- Set client delivery values in that project's DELIVERY-PROFILE (GSD: `.gsd/DELIVERY-PROFILE.md`; W2C: `.w2c/DELIVERY-PROFILE.md`) — clean profile only, never append `CLAUDE.md` / `AGENTS.md` wrapper content; do not customize shared templates for a single client; multi-app monorepos may document path-conditional integration branches in Notes rather than a single Active branch; keep W2C `git_delivery` in `.w2c/config.toml` cadence-only (slice vs milestone commit/push/PR); grill Integration strategy (trunk-direct | feature-branch | gitflow — prefer gitflow for mobile store apps) and optional CI/CD pointers independently so projects can choose gitflow-only, CI-only, both, or neither.
- Prefer a personal global skills hub at `~/.agents/skills` (Cursor/Claude bridges; optional Codex) with playbook `shared/gsd` as SoT for GSD/workflow skills and https://github.com/OpenW2C/w2c for work-to-chores / do-chores; add/update skills via `shared/gsd/ADDING-SKILLS.md` and `scripts/install-personal-agents-hub.sh` / `update-personal-skill.sh` (manifest: `shared/gsd/personal-skills.manifest`; W2C skills via OpenW2C/w2c `install.sh` / `w2c install-skills`); no multi-template skill forks — platform specifics via `shared/gsd/templates/platforms/` packs; overlay `.cursor/skills` must not ship GSD/do-next skill dirs (even stubs) — W2C/none clients symlink those trees.

## Learned Workspace Facts

- This repository is the source template for client AI overlays (`ios/`, `android/`, `flutter-riverpod/`, `flutter-bloc/`, `universal/`), not a typical application codebase. Cursor multi-root `ai-playbook.code-workspace` may include the sibling W2C checkout (`../w2c`). Portable mobile CI/CD docs (gitflow, secrets obtainment, TestFlight/Play pointers) live under `mobile/ci-cd/` and are linked from DELIVERY-PROFILE when CI pointers are opted in — not embedded in w2c core.
- Client overlay install uses `scripts/install-client-ai-overlay.sh` with `--platform` and typically `--mode symlink`.
- Overlay install pattern: playbook files are symlinked as `_AGENTS.md`, `_CLAUDE.md`, `_ARCHITECTURE.md`, `_SESSION_WORKFLOW.md` (architecture-only — no planning-engine text); committed wrappers include those and hold project-specific, selected-engine, and continual-learning sections. Overlay `AGENTS.md` templates must not contain another client's learned facts or unselected engine names (GSD, graphify, ruflo).
- The framework unifies Cursor, Claude Code, and VS Code Copilot around local-first engines: gsd-pi, graphify, and ruflo.
- Personal Agents Hub skills (do-next, do-next-runner, ticket-to-plan, graphify-obsidian, etc.) live under `shared/gsd/`; GSD-free work-to-chores / do-chores live in the sibling checkout `/Users/khandkermahmudur/Workspace/self/w2c` and https://github.com/OpenW2C/w2c (playbook `shared/w2c` is gone; do not author W2C under `shared/gsd/`; W2C skills and `.w2c/` docs must not mention GSD). Install/refresh with that repo’s `install.sh` (curl) or `pipx install git+https://github.com/OpenW2C/w2c.git`, then `w2c install-skills`. `sync-gsd-skills-to-overlays.sh` is retired and refuses. Platform overlay skill trees must not contain GSD/do-next directories (even `platform.md` stubs).
- `scripts/repair-after-ruflo.sh` re-applies playbook hook-handler safety after `ruflo init` and installs Cursor-safe Ruflo hooks from `config/cursor-hooks/` via `scripts/install-ruflo-cursor-hooks.sh` (adapter wraps handler when JSON `permissionDecision` on stdout is required). Canonical handler SoT is repo-root `.claude/helpers/hook-handler.cjs`; after editing it, run `scripts/sync-hook-handler-platforms.sh` then `scripts/verify-hook-safety.sh` so platform overlay copies (`universal`/`ios`/`android`/`flutter-*`) stay in sync (drift breaks repair installs).
- After `gsd_plan_slice` / ticket-to-plan, gsd-pi Attempt-based completion needs a running Attempt; playbook ships `shared/gsd/mcp/gsd-external-executor` (`playbook_gsd_task_begin` → `gsd_task_complete` → `playbook_gsd_task_publish` / abort) so do-next can advance the ledger without unsupervised `gsd_execute`.
- On platform templates, `.claude/skills/<name>` are typically directory symlinks to `../../.cursor/skills/<name>`; Copilot `.github/instructions/*.instructions.md` are real files that must be synced separately.
- Playbook skills are authored under `shared/gsd/` (flat `SKILL.md` or assembled `SKILL.body.md`); client GSD install via `shared/gsd/scripts/install-workflow-tools.sh --project` (flat orphans need `--tools`). Client Copilot + W2C ledger: `w2c init` / `w2c init --track` after installing https://github.com/OpenW2C/w2c. `w2c` on PATH (`~/.local/bin/w2c`) is the only writer of STATE/QUEUE/ROADMAP emojis and task checkboxes. Default gitignore is `.w2c/` plus Copilot W2C instruction files unless `track = true`; existing tracked ledgers use `w2c migrate untrack` / `adopt`. Machine CLI data: `~/.local/share/w2c`; runtime templates always load from `~/.local/share/w2c/templates` (respect `W2C_DATA_HOME` / `XDG_DATA_HOME`), never the git checkout; global registry: `~/.config/.w2c/config.toml`. Do not symlink playbook `.w2c/scripts` into clients; agents use PATH `w2c` and must not glob-search for `w2c.py`. W2C event/audit logs stay machine-local and must not be committed. Milestone/slice plans include a Git Operation Plan (`w2c smoke` fails without isolation fields): worktree or branch only; plan in the current checkout; first `do-chores` creates or reuses one isolation per ticket (local==remote==ticket id or confirmed slug); commit `.w2c/` plan artifacts only when `track = true`; otherwise symlink/copy `.w2c/` into worktrees; hard-stop on dirty or unexpected git state; worktree mode uses `using-git-worktrees`. Milestone and slice plans also carry repo-detected Commit and PR conventions (forbid AI `Co-authored-by` trailers; smoke-enforced). Optional milestone `M###-MANUAL-TEST.md` is offered at planning time and must not gate milestone-complete. `M###-VALIDATION.md` / `M###-SUMMARY.md` (and slice UAT/SUMMARY) are agent-authored closeout reports from templates—not written by the CLI; `w2c` only emits `NEED_*_REPORTS` when ROADMAP slices use the checkbox list shape it can parse. `w2c init` must not stub empty `CONTEXTvN.md`; fill the latest stub or skip, and bump only when the latest CONTEXT has real content.
- Preferred client bring-up: playbook-operator `configure-client-project` (exclusive engine: GSD, W2C, or none). GSD: `scripts/configure-client-check.sh` preflight, grill-style delivery interview, then overlay + `bootstrap-gsd-workflow.sh`; install via `scripts/update-personal-skill.sh configure-client-project`. Manual GSD fallback: `install-client-ai-overlay.sh` then `bootstrap-gsd-workflow.sh` with `--patch-mcp --with-do-next` (and `--harness-context` when useful); skip `--init-gsd` if client already has `gsd.db`. `--patch-mcp` does not overwrite existing `.mcp.json`; flag missing `playbook-gsd` and other gaps and ask whether to apply each. `--with-do-next` copies `playbook-gsd-health.sh` into client `.workflow/scripts/`. stderr warnings for `GSD_WORKFLOW_EXECUTORS_MODULE` / `GSD_WORKFLOW_WRITE_GATE_MODULE` are expected. Re-bootstrap without `--force` will not overwrite an existing `DELIVERY-PROFILE.md`. W2C: require `w2c` on PATH, then `w2c init` (not wrappers-only). None: overlay only. W2C/none configure passes `--no-require-gsd`. `--engine w2c` restamps wrappers and installs W2C but does not delete leftover `.gsd` / GSD MCP / `.workflow` and does not migrate GSD plans; treat uninstall and any GSD→W2C port (into executable `.w2c/plans/M###-*/` shape) as separate in-session gaps — `do-chores` will not run files under `.w2c/imported-from-gsd/`.
- `.gsd` is often a symlink to `~/.gsd/projects/<id>`; path jails must allow that resolved store. Machine `.mcp.json` / `.workflow/` copies are local; committed SoTs are `config/mcp.template.json` and `shared/gsd/scripts/playbook-gsd-health.sh`. VS Code Copilot strictly validates MCP tool schemas (`array` needs `items`); `gsd-browser` tools like `browser_assert` can abort Copilot chat — omit `gsd-browser` from Copilot MCP config (Cursor-only) until upstream fixes.
- If lean-ctx reports it is rooted elsewhere, the MCP jail is bound to another workspace; reload or re-enable the lean-ctx MCP (or add `extra_roots`) so `ctx_*` matches the open repo.
