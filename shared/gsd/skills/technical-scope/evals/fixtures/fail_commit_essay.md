Commit message:

Add a version check so the installed copy can be compared with the playbook copy. The ordinary web protocol (HTTP) is not involved. Here is the full design. The checker reads the playbook file, reads the installed file, and prints whether they match. Then it walks every bridge, and each bridge is a shortcut from one folder to another. Then it explains why a real folder instead of a shortcut is a failure. Then it explains the history of the skills folder, the people who might run the command, and the afternoon on which the first copy was made. Then it lists every sentence the command might print, and what the reader should feel when reading them. Then it repeats the design in different words so the message fills a page. The repository already forbids inventing a co-author trailer, and this message does not add one.
